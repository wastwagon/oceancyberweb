/** Content sections for OceanCyber POS Development Guide PDF v5.0 */
export const GUIDE_VERSION = "5.0";
export const GUIDE_DATE = "2026-07-08";

export const SECTIONS = [
  {
    title: "1. Product vision & positioning",
    body: [
      "OceanCyber POS is a **complete multi-tenant SaaS** for Ghanaian operators: self-service registration, subscription billing, configurable payments (merchant's own Paystack/Hubtel keys or offline/cash), offline-first POS, and multi-branch control.",
      "",
      "Market anchor: from GHS 99/month (SellarPro, CliqPOS, Retill, Konect benchmark).",
      "Premium edge: optional assisted onboarding for Enterprise — self-serve is the default path.",
      "Dual acquisition: Start free trial (self-serve) OR Book demo (sales-assisted) from marketing site.",
      "Currency: GHS only in UI and reports; OceanCyber subscription billed separately from in-store customer payments.",
      "Product app: app.pos.oceancyber.net — separate from oceancyber.net marketing site.",
    ],
  },
  {
    title: "2. Scope boundary",
    body: [
      "IN SCOPE (POS SaaS product):",
      "• Self-service signup, email verification, onboarding wizard",
      "• SaaS subscription billing (OceanCyber Paystack)",
      "• Merchant BYOK payment config (Paystack, Hubtel) OR offline/manual MoMo + cash",
      "• Electron/tablet clients, admin portal, inventory, multi-branch, restaurant module",
      "",
      "Marketing site (oceancyber.net): product pages linking to app.pos.oceancyber.net/signup — no POS app hosted there.",
      "OUT OF SCOPE on marketing site: POS login UI, tenant admin, payment key storage.",
    ],
  },
  {
    title: "3.1 Marketing spec — checkout",
    body: [
      "• Barcode scan + SKU search + quick keys (top sellers)",
      "• Line-item and cart-level discounts (supervisor PIN above threshold)",
      "• Tax lines: NHIL/GETFund/VAT configurable per tenant (Ghana retail norms)",
      "• Payments: Cash, Card (terminal phase 2), Automated MoMo (tenant Paystack/Hubtel), Manual MoMo, Telecel/AT",
      "• Change due calculation for cash",
      "• Receipt: 80mm thermal ESC/POS + optional SMS/email",
      "• Shift: open float, close count, variance report",
      "• Hold/recall cart for busy counters",
    ],
  },
  {
    title: "3.2 Marketing spec — offline sync",
    body: [
      "• SQLite on device: catalog snapshot + pending operations queue",
      "• Sale allowed offline up to configurable limit (default 72h sync window)",
      "• Operations: SALE_CREATE, SALE_VOID, PAYMENT_ADD, STOCK_ADJUST, SHIFT_CLOSE",
      "• Client sequence: monotonic per (device_id); server idempotency key = client UUID",
      "• UI: green/yellow/red sync indicator; pending count badge",
      "• On reconnect: batch upload → server ack → download catalog delta",
      "• Conflict: duplicate client UUID rejected; stock conflicts → supervisor queue",
    ],
  },
  {
    title: "3.3 Marketing spec — inventory & branches",
    body: [
      "INVENTORY: SKU, barcode, category, cost, sell price, reorder level, supplier ref.",
      "Stock moves: sale decrement, GRN (goods received), transfer out/in, adjustment with reason.",
      "Alerts: email/SMS to branch manager when below reorder level.",
      "",
      "MULTI-BRANCH: branch entity with address, timezone, tax profile.",
      "HQ can push price updates; branch manager can request local override (approval workflow optional).",
      "Dashboards: today vs yesterday, payment mix pie, top 10 SKUs, hourly heatmap.",
    ],
  },
  {
    title: "3.4 Marketing spec — restaurant & onboarding",
    body: [
      "RESTAURANT: floor plans, table states (free/occupied/bill requested), send-to-kitchen, KOT print, split by seat or amount.",
      "",
      "MOBILE SUPERVISOR: PWA or RN app — approve voids, view live sales, low stock push.",
      "",
      "ONBOARDING PACK (OceanCyber delivery):",
      "1. Discovery form (vertical, branches, payment methods, hardware)",
      "2. Catalog import CSV/Excel template",
      "3. Device pairing session",
      "4. 2h staff training + printed quick-start",
      "5. Launch week WhatsApp support group",
    ],
  },
  {
    title: "4. Industry vertical presets",
    body: [
      "Each preset seeds demo catalog, tax defaults, and report labels:",
      "",
      "Retail — barcode-heavy, weigh-scale optional, loyalty card field.",
      "Tourism/hospitality — room charge folio link (phase 3), restaurant tables enabled.",
      "Healthcare/pharmacy — batch/expiry on SKU, controlled discount limits.",
      "Financial/agency — transaction caps, mandatory ID field on sale.",
      "Agriculture — bulk unit (bag, crate) conversions.",
      "Logistics — depot counter, driver float tracking.",
      "Real estate — service SKUs (maintenance fees), invoice-style receipt.",
      "Media/events — timed sessions, combo tickets.",
    ],
  },
  {
    title: "5. Pricing tiers & feature gates",
    body: [
      "Starter GHS 99/mo — 1 branch, 3 users, 1 device, cash+MoMo, offline, basic reports.",
      "Growth GHS 199/mo — 3 branches, 10 users, 5 devices, inventory+PO, SMS receipts.",
      "Multi-branch GHS 299/mo — 10 branches, 25 users, transfers, HQ dashboard, API read access.",
      "Enterprise custom — unlimited, GRA E-VAT module, SLA 99.9%, dedicated CSM, custom integrations.",
      "",
      "Hardware kits quoted separately (GHS 800–3,500+): tablet, Bluetooth printer, scanner.",
      "Annual prepay: 10 months price for 12 months.",
      "Enforce limits in API middleware (branch count, device count, user seats).",
    ],
  },
  {
    title: "6. Recommended tech stack",
    body: [
      "Monorepo suggestion: oceancyber-pos/ with apps/pos-desktop, apps/pos-admin, apps/api, packages/shared.",
      "",
      "POS desktop: Electron 28+ or Tauri 2 + React 18 + TypeScript + Tailwind.",
      "POS tablet: React Native (shared TS business logic) or responsive PWA for pilot.",
      "Admin portal: Next.js 14 or Vite+React — tenant HQ configuration.",
      "API: NestJS 10 (aligns with OceanCyber team) — REST + Socket.io for live dashboard.",
      "DB: PostgreSQL 16 — multi-tenant row isolation.",
      "Cache/queue: Redis 7 + BullMQ — webhooks, SMS, sync batch processing.",
      "Local: SQLite via better-sqlite3 (desktop) or expo-sqlite (mobile).",
      "Payments: Paystack + Hubtel aggregators first; direct MTN/Telecel APIs at enterprise volume.",
      "SMS: Hubtel or Africa's Talking.",
      "Observability: Sentry, OpenTelemetry, structured JSON logs (pino).",
      "CI: GitHub Actions — lint, test, build artifacts, Electron code sign (Windows/macOS).",
    ],
  },
  {
    title: "7. Repository layout (proposed)",
    body: [
      "oceancyber-pos/",
      "  apps/api/           — NestJS multi-tenant API",
      "  apps/admin/         — HQ web portal",
      "  apps/pos-desktop/   — Electron register client",
      "  apps/pos-mobile/    — RN supervisor app (phase 2)",
      "  packages/shared/    — DTOs, enums, sync protocol types",
      "  packages/receipt/   — ESC/POS builder",
      "  prisma/             — schema + migrations",
      "  docker-compose.yml  — postgres, redis, api, admin",
      "  docs/               — OpenAPI spec, this guide",
      "",
      "Separate Git repo from oceancyber.net marketing monorepo.",
    ],
  },
  {
    title: "8. System architecture",
    body: [
      "Clients ←HTTPS/WSS→ API Gateway (TLS, rate limit) → NestJS modules.",
      "PostgreSQL (primary), Redis (cache, queues, session).",
      "Workers: payment-webhook, sms-send, sync-batch, report-aggregate (nightly cron).",
      "",
      "Device boot flow: login → fetch tenant config → download catalog snapshot (version hash) → register device heartbeat every 60s.",
      "Catalog updates: HQ publishes → increment catalog_version → clients pull delta on next sync.",
      "",
      "Admin portal talks to same API with user JWT (not device token).",
      "Marketing site does NOT call POS API publicly — leads go to oceancyber.net CRM/contact pipeline.",
    ],
  },
  {
    title: "9. Database schema — core tenancy",
    body: [
      "tenants — id, name, slug, plan_tier, status, timezone, currency, created_at",
      "branches — id, tenant_id, name, code, address, phone, is_active",
      "users — id, tenant_id, email, password_hash, role, branch_id (nullable for HQ)",
      "devices — id, tenant_id, branch_id, name, device_token_hash, last_seen_at, app_version",
      "subscriptions — id, tenant_id, plan, amount_ghs, paystack_sub_code, renews_at, status",
      "",
      "All business tables include tenant_id UUID NOT NULL + index (tenant_id, ...).",
      "Use Prisma or Drizzle ORM; enforce RLS optional second layer in PostgreSQL.",
    ],
  },
  {
    title: "9. Database schema — catalog & inventory",
    body: [
      "categories — id, tenant_id, name, parent_id, sort_order",
      "products — id, tenant_id, sku, barcode, name, category_id, unit, tax_rate_id, is_active",
      "product_prices — id, product_id, branch_id (null=HQ default), price_ghs, effective_from",
      "stock_levels — id, tenant_id, branch_id, product_id, quantity, reserved_qty",
      "stock_movements — id, tenant_id, branch_id, product_id, delta, reason, ref_type, ref_id, user_id",
      "suppliers — id, tenant_id, name, phone, email",
      "purchase_orders — id, tenant_id, branch_id, supplier_id, status, ordered_at",
      "po_lines — id, po_id, product_id, qty_ordered, qty_received, unit_cost",
      "branch_transfers — id, from_branch, to_branch, status, shipped_at, received_at",
    ],
  },
  {
    title: "9. Database schema — sales & payments",
    body: [
      "shifts — id, tenant_id, branch_id, device_id, user_id, opened_at, closed_at, opening_float, closing_cash",
      "sales — id, tenant_id, branch_id, device_id, shift_id, client_uuid, receipt_no, status, subtotal, tax, total, created_at",
      "sale_lines — id, sale_id, product_id, qty, unit_price, discount, line_total",
      "payments — id, sale_id, method (cash|momo|card|telecel|at), amount, status, external_ref, idempotency_key",
      "momo_transactions — id, payment_id, provider, msisdn_masked, telco_ref, callback_payload_json",
      "voids — id, sale_id, reason, approved_by, voided_at",
      "refunds — id, sale_id, amount, method, approved_by (phase 2)",
      "",
      "receipt_no: branch prefix + sequential per branch (e.g. ACC-20260708-0042).",
    ],
  },
  {
    title: "9. Database schema — restaurant & sync",
    body: [
      "tables — id, branch_id, floor_id, label, seats, status",
      "floors — id, branch_id, name, layout_json",
      "table_orders — id, table_id, sale_id, status (open|kitchen|served|paid)",
      "kitchen_tickets — id, table_order_id, station, items_json, printed_at",
      "",
      "sync_batches — id, device_id, uploaded_at, op_count, status",
      "sync_operations — id, batch_id, client_seq, op_type, payload_json, server_applied_at, conflict_flag",
      "catalog_snapshots — id, tenant_id, version, manifest_json, created_at",
    ],
  },
  {
    title: "10. REST API — auth & tenancy",
    body: [
      "POST /api/v1/auth/register — self-serve signup (creates user + tenant)",
      "POST /api/v1/auth/verify-email — activate account",
      "POST /api/v1/auth/login — email/password → user JWT + refresh",
      "POST /api/v1/auth/device/pair — pairing code from admin → device token",
      "POST /api/v1/auth/refresh — rotate tokens",
      "GET  /api/v1/me — user profile + permissions + tenant status",
      "GET  /api/v1/tenant — tenant config, plan limits, feature flags, onboarding_step",
      "",
      "Headers: Authorization Bearer (user or device), X-Tenant-Id (optional if embedded in JWT).",
      "Device tokens scoped: branch_id, device_id, permissions cashier-only.",
    ],
  },
  {
    title: "10. REST API — catalog & sync",
    body: [
      "GET  /api/v1/catalog/snapshot?since_version=N — full or delta catalog",
      "GET  /api/v1/products — paginated search (admin)",
      "POST /api/v1/products — create (admin, branch_manager+)",
      "PATCH /api/v1/products/:id — update",
      "POST /api/v1/sync/push — body: { device_id, operations[] }",
      "GET  /api/v1/sync/status — pending conflicts for supervisor",
      "POST /api/v1/sync/resolve — approve/reject conflict",
    ],
  },
  {
    title: "10. REST API — sales & payments",
    body: [
      "POST /api/v1/shifts/open — opening float",
      "POST /api/v1/shifts/close — closing count",
      "POST /api/v1/sales — create sale (online) or via sync push (offline)",
      "GET  /api/v1/sales/:id — receipt detail",
      "POST /api/v1/sales/:id/void — supervisor approval",
      "POST /api/v1/payments/manual-momo — manual MoMo record (no API)",
      "POST /api/v1/payments/momo/initiate — automated MoMo (tenant's Paystack/Hubtel keys)",
      "GET  /api/v1/payments/momo/:id/status — poll",
      "POST /api/v1/webhooks/paystack/{tenant_slug} — in-store payments (tenant secret)",
      "POST /api/v1/webhooks/hubtel/{tenant_slug} — in-store payments (tenant secret)",
      "POST /api/v1/billing/webhooks/paystack — OceanCyber SaaS subscription (platform secret)",
    ],
  },
  {
    title: "10. REST API — inventory, reports, admin",
    body: [
      "GET/POST /api/v1/stock/levels — branch stock",
      "POST /api/v1/stock/adjust — reason required",
      "POST /api/v1/purchase-orders — create PO",
      "POST /api/v1/transfers — branch transfer",
      "",
      "GET /api/v1/reports/daily?branch_id&date — sales summary",
      "GET /api/v1/reports/payment-mix — by method",
      "GET /api/v1/reports/top-products — limit 20",
      "GET /api/v1/reports/export.csv — finance export",
      "",
      "POST /api/v1/admin/tenants — OceanCyber internal provision",
      "POST /api/v1/admin/tenants/:id/impersonate — support only, audited",
    ],
  },
  {
    title: "11. WebSocket events (live dashboard)",
    body: [
      "Channel: tenant:{id}:branch:{id}",
      "",
      "sale.created — { sale_id, total, method }",
      "payment.momo.pending — { sale_id, msisdn_masked }",
      "payment.momo.success | payment.momo.failed",
      "sync.conflict — { device_id, op_type }",
      "stock.low — { product_id, qty, reorder_level }",
      "",
      "Admin dashboard subscribes on login; branch managers see own branch only.",
    ],
  },
  {
    title: "12. MoMo payment state machine",
    body: [
      "States: INITIATED → PROMPT_SENT → AWAITING_OTP → SUCCESS | FAILED | TIMEOUT | CANCELLED",
      "",
      "Flow:",
      "1. Cashier selects MoMo, enters customer MSISDN (233XXXXXXXXX)",
      "2. API creates payment row + idempotency_key (UUID)",
      "3. Call Paystack/Hubtel charge API with callback URL",
      "4. Customer approves on phone; webhook arrives",
      "5. Verify signature; match idempotency_key; mark sale PAID; print receipt",
      "",
      "Timeout: 120s default → offer retry or switch to cash.",
      "Duplicate webhook: idempotent — return 200 without double-settling.",
      "Store raw callback JSON for disputes (90-day retention minimum).",
    ],
  },
  {
    title: "13. Offline sync protocol (detail)",
    body: [
      "Operation envelope: { client_uuid, client_seq, op_type, payload, device_timestamp }",
      "",
      "Server processing order: sort by client_seq per device; reject gaps with RESYNC_REQUIRED.",
      "SALE_CREATE payload: lines[], payments[], shift_id, client_receipt_no.",
      "Idempotency: client_uuid unique index — replay returns original server sale_id.",
      "",
      "Catalog delta: { version, products_added[], products_updated[], products_removed[] }.",
      "Device stores catalog_version locally; skip full download if version match.",
      "",
      "Max offline sales without sync: configurable 500 ops or 72h — then block new sales until sync.",
    ],
  },
  {
    title: "14. UI screens — POS desktop client",
    body: [
      "Login / device pairing",
      "Shift open (float entry)",
      "Main sell — search, cart, numpad, payment drawer",
      "Payment — method tabs, MoMo MSISDN, waiting spinner",
      "Receipt preview + print",
      "Shift close — count cash, variance",
      "Supervisor override PIN modal",
      "Sync status drawer",
      "Settings — printer test, logout",
      "",
      "Brand UI: dark #0c0c10, accent lime #bbf340, large touch targets (min 44px), high contrast for shop floor.",
    ],
  },
  {
    title: "14. UI screens — admin portal",
    body: [
      "Dashboard — KPI cards, payment mix chart, branch selector",
      "Products — grid CRUD, import CSV, barcode print",
      "Inventory — levels, PO list, transfer wizard",
      "Branches & devices — pairing QR/code",
      "Users & roles — invite email",
      "Reports — date range, export",
      "Settings — tax rates, receipt footer, MoMo credentials (encrypted)",
      "Billing — plan, usage vs limits (phase 2 Paystack sub)",
    ],
  },
  {
    title: "15. Security requirements",
    body: [
      "TLS 1.2+ everywhere; HSTS on admin and API.",
      "Passwords: bcrypt/argon2; lockout after 5 failures.",
      "Device tokens: long random, rotatable, stored hashed server-side.",
      "PII: mask MSISDN in logs (233XX***XX12); encrypt MoMo API keys at rest (AES-256).",
      "RBAC enforced in guards + DB queries scoped by tenant_id.",
      "Audit log: voids, price changes, role changes, admin impersonation.",
      "Webhook signatures mandatory in production — reject unsigned.",
      "Ghana DPA: privacy policy, data retention config, export/delete on request (enterprise).",
      "Pen test before enterprise tier launch.",
    ],
  },
  {
    title: "16. Environment variables (product)",
    body: [
      "DATABASE_URL, REDIS_URL",
      "JWT_SECRET, JWT_REFRESH_SECRET, DEVICE_TOKEN_PEPPER",
      "API_PUBLIC_URL, ADMIN_PUBLIC_URL, CORS_ORIGINS",
      "PAYSTACK_SECRET_KEY, PAYSTACK_WEBHOOK_SECRET",
      "HUBTEL_CLIENT_ID, HUBTEL_CLIENT_SECRET, HUBTEL_WEBHOOK_SECRET",
      "SMS_API_KEY, SMS_SENDER_ID",
      "SENTRY_DSN, NODE_ENV",
      "TENANT_DEFAULT_TIMEZONE=Africa/Accra",
      "CURRENCY_DEFAULT=GHS",
      "OFFLINE_MAX_HOURS=72",
      "MOMO_TIMEOUT_SECONDS=120",
      "OCEANCYBER_INTERNAL_API_KEY — provisioning from CRM (optional)",
    ],
  },
  {
    title: "17. Local development setup",
    body: [
      "git clone git@github.com:oceancyber/oceancyber-pos.git",
      "cp .env.example .env",
      "docker compose up -d postgres redis",
      "npm install",
      "npx prisma migrate dev",
      "npm run seed — demo tenant + products",
      "npm run dev:api — http://localhost:4200",
      "npm run dev:admin — http://localhost:4201",
      "npm run dev:desktop — Electron hot reload",
      "",
      "Demo credentials in seed: demo@merchant.gh / (from SEED_PASSWORD env).",
      "Paystack test keys from dashboard; use test MSISDN from Paystack docs.",
    ],
  },
  {
    title: "18. Deployment — Coolify VPS",
    body: [
      "Separate Coolify resource from oceancyber.net marketing stack.",
      "",
      "Services: postgres, redis, api (port 4000), admin (port 3000).",
      "Domains: api.pos.oceancyber.net → api; admin.pos.oceancyber.net → admin.",
      "Env: all secrets in Coolify UI; never commit .env.",
      "COMPOSE_PARALLEL_LIMIT=1 on 4GB VPS during first build.",
      "Postgres backups: daily pg_dump to object storage.",
      "Desktop clients: download from releases.pos.oceancyber.net or auto-update S3/R2.",
      "",
      "Do NOT merge POS API into marketing docker-compose.yml without architecture review.",
    ],
  },
  {
    title: "19. Phase 0 sprint backlog (weeks 1–6)",
    body: [
      "S0-1 Monorepo scaffold, CI, Docker postgres/redis",
      "S0-2 Prisma schema: tenants, branches, users, devices",
      "S0-3 Auth: login, JWT refresh, device pairing code",
      "S0-4 Products CRUD API + admin UI list/create",
      "S0-5 Catalog snapshot endpoint + version hash",
      "S0-6 Electron shell: login, catalog download, empty cart UI",
      "S0-7 Sale create API (cash only) + receipt number seq",
      "S0-8 Shift open/close API + desktop flows",
      "S0-9 Thermal print ESC/POS proof of concept",
      "S0-10 Seed script + internal admin tenant provision CLI",
    ],
  },
  {
    title: "20. Phase 1 MVP backlog (weeks 7–16)",
    body: [
      "S1-1 MoMo initiate + Paystack webhook + polling fallback",
      "S1-2 SQLite offline queue on desktop + sync push endpoint",
      "S1-3 Idempotency + conflict detection",
      "S1-4 SMS receipt via Hubtel",
      "S1-5 Supervisor PIN void/discount",
      "S1-6 Daily report API + admin dashboard charts",
      "S1-7 Stock decrement on sale + basic stock levels",
      "S1-8 Pilot merchant #1 onboarding (real catalog)",
      "S1-9 Load test 50 concurrent sales/min per branch",
      "S1-10 Beta release desktop installer (Windows primary, Ghana market)",
    ],
  },
  {
    title: "21. Phase 2 growth backlog (weeks 17–24)",
    body: [
      "S2-1 Multi-branch HQ dashboard roll-up",
      "S2-2 Branch transfers + PO workflow",
      "S2-3 Telecel Cash provider adapter",
      "S2-4 Restaurant tables + kitchen ticket print",
      "S2-5 Plan tier enforcement middleware",
      "S2-6 Paystack subscription billing linkage",
      "S2-7 CSV export for accountants",
      "S2-8 Industry preset import CLI",
      "S2-9 Supervisor mobile PWA",
      "S2-10 Public marketing site integration — demo request → CRM ticket auto-create",
    ],
  },
  {
    title: "22. Phase 3 enterprise backlog",
    body: [
      "E-1 GRA E-VAT certified integration (legal gate)",
      "E-2 Card terminal integration (Verifone/Ingenico partner)",
      "E-3 Direct MTN API at volume tier",
      "E-4 Advanced analytics — cohort, basket analysis",
      "E-5 Hardware kit fulfillment partner workflow",
      "E-6 99.9% SLA monitoring + status page",
      "E-7 SSO for enterprise HQ (Google/Microsoft)",
      "E-8 Read-only API for ERP/accounting export",
    ],
  },
  {
    title: "23. Testing strategy",
    body: [
      "Unit: tax calculation, change due, idempotency merge, receipt number format.",
      "Integration: Supertest on NestJS — sale + payment webhook path.",
      "E2E: Playwright on admin; Spectron/Playwright on Electron for sell flow.",
      "MoMo: Paystack sandbox + real device on staging before prod keys.",
      "Offline: automated test — queue 20 ops offline, sync, assert ledger balance.",
      "Load: k6 script 100 RPS sync push sustained 5 min.",
      "UAT: pilot merchant sign-off checklist per vertical preset.",
    ],
  },
  {
    title: "24. GRA E-VAT & compliance roadmap",
    body: [
      "V1: configurable tax components; export CSV labeled for manual GRA filing.",
      "V2: e-invoice UUID generation internal only (staging).",
      "V3: GRA-certified submission API after third-party audit — update marketing only when certified.",
      "",
      "Ghana Data Protection Act: register as data controller if >1000 customer records across tenants.",
      "Receipt retention default 7 years configurable; MSISDN retention 90 days unless loyalty opt-in.",
      "Terms of service: merchant is controller for end-customer PII; OceanCyber is processor.",
    ],
  },
  {
    title: "25. Acquisition & onboarding paths",
    body: [
      "PATH A — Self-serve SaaS (primary):",
      "1. app.pos.oceancyber.net/signup",
      "2. Verify email → onboarding wizard (business, branch, plan, payments, device)",
      "3. Start trial or subscribe (OceanCyber billing)",
      "4. Configure own Paystack/Hubtel OR use cash + manual MoMo only",
      "5. Go live",
      "",
      "PATH B — Sales-assisted (Enterprise / complex):",
      "1. oceancyber.net contact / demo request",
      "2. OceanCyber admin pre-provisions tenant or assists through wizard",
      "3. Same SaaS product — optional white-glove catalog import + training",
      "",
      "CRM fields: acquisition_channel (self_serve|demo|partner), utm_source, plan, CSM owner.",
    ],
  },
  {
    title: "26. Marketing ↔ engineering alignment",
    body: [
      "Before each release, verify marketed features:",
      "□ Mockup screen exists in product or is marked roadmap",
      "□ Tier gate documented in admin feature flags",
      "□ MoMo tested MTN + Telecel sandbox wallets",
      "□ Offline demo script rehearsed for sales",
      "□ All amounts GHS — no USD in UI",
      "□ GRA claims match certification status",
      "□ Support SLA documented per tier",
      "□ Known limitations listed in demo script (e.g. card terminal phase 2)",
    ],
  },
  {
    title: "27. Competitive differentiation (Ghana 2026)",
    body: [
      "Table stakes: MoMo, offline, multi-branch, GHS pricing, local support.",
      "",
      "OceanCyber wins on:",
      "• White-glove onboarding (agency-grade delivery)",
      "• Security-aware architecture (audit logs, encryption)",
      "• Cross-sell to web/mobile/custom builds for merchants outgrowing POS",
      "• ThinQ/commerce case study credibility",
      "",
      "Do not race to lowest price — match GHS 99 entry but sell Growth/Multi-branch on implementation value.",
    ],
  },
  {
    title: "28. Open decisions (log before build)",
    body: [
      "OD-1 Electron vs Tauri for desktop (Tauri = smaller binary, Electron = faster MVP).",
      "OD-2 Paystack vs Hubtel primary MoMo aggregator.",
      "OD-3 Single repo vs api repo + client repo.",
      "OD-4 Card terminal partner and timeline.",
      "OD-5 GRA E-VAT partner/certification body.",
      "OD-6 RESOLVED: Self-serve SaaS signup is required — see sections A–O.",
      "OD-7 Franchise pricing model for resellers.",
      "",
      "Record decisions in docs/pos/ADR/ folder as Architecture Decision Records.",
    ],
  },
];
