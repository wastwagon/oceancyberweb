/** SaaS self-serve & payment configuration — OceanCyber POS Guide v4.0 */
export const SAAS_SECTIONS = [
  {
    title: "A. Complete SaaS model (overview)",
    body: [
      "OceanCyber POS is a **full multi-tenant SaaS product**: merchants **self-register**, complete an onboarding wizard, choose a plan, and configure their own environment — including **their own payment providers** or **offline-only** mode.",
      "",
      "Two payment layers (never confuse them):",
      "1. **SaaS subscription** — merchant pays OceanCyber (GHS 99+/mo) via Paystack for using the software.",
      "2. **In-store payments** — merchant's customers pay the merchant via cash, offline MoMo, or merchant-configured Paystack/Hubtel/telco keys.",
      "",
      "OceanCyber does NOT take a cut of in-store MoMo transactions unless explicitly offered as a future aggregator tier.",
      "Premium **assisted onboarding** remains available for Enterprise — but is optional, not required to start.",
    ],
  },
  {
    title: "B. Self-service registration flow",
    body: [
      "Public URLs (POS product domain, not marketing site):",
      "• app.pos.oceancyber.net/signup — create account",
      "• app.pos.oceancyber.net/signin — returning users",
      "• app.pos.oceancyber.net/onboarding — post-signup wizard (auth required)",
      "",
      "Registration fields:",
      "• Full name, work email, phone (233…), password (min 12 chars)",
      "• Business / trading name",
      "• Accept Terms of Service + Privacy Policy + DPA processor clause",
      "",
      "Flow:",
      "1. POST /api/v1/auth/register → create user + tenant (status: pending_verification)",
      "2. Send verification email (magic link or 6-digit code, 15 min TTL)",
      "3. GET /api/v1/auth/verify-email?token=… → activate tenant (status: trialing or active)",
      "4. Redirect to onboarding wizard (5 steps)",
      "5. On wizard complete → tenant status: active, first branch + admin user ready",
      "",
      "Rate limit: 5 registrations/hour per IP; captcha (hCaptcha/Turnstile) on signup.",
    ],
  },
  {
    title: "C. Onboarding wizard (5 steps)",
    body: [
      "Step 1 — Business profile: legal name, TIN (optional), industry preset, timezone Africa/Accra.",
      "Step 2 — First branch: name, address, phone, receipt footer text.",
      "Step 3 — Plan selection: Starter / Growth / Multi-branch (14-day trial on Starter optional).",
      "Step 4 — Payment setup (see section D): configure providers OR offline-only.",
      "Step 5 — Invite team + pair first device (QR pairing code, 10 min expiry).",
      "",
      "Skip allowed: Steps 4–5 skippable — default to **cash-only offline mode** until merchant configures payments later in Settings.",
      "Progress saved per step; resume on re-login.",
      "Empty catalog: offer sample products import or CSV upload template download.",
    ],
  },
  {
    title: "D. Merchant payment configuration (BYOK)",
    body: [
      "Merchants configure **their own** payment credentials in Settings → Payments.",
      "OceanCyber stores secrets **encrypted** (AES-256-GCM, tenant-specific data key in KMS/env).",
      "Never return secret keys to client after save — only masked preview (pk_live_…xxxx).",
      "",
      "Supported provider modules (enable/disable per tenant):",
      "• **Cash** — always available",
      "• **Offline MoMo** — record MoMo payment manually (reference number, no API) — see section E",
      "• **Paystack** — merchant's public + secret key; subaccount optional",
      "• **Hubtel** — client id + secret + merchant account number",
      "• **Direct telco** — Enterprise roadmap (MTN/Telecel API credentials)",
      "• **Card terminal** — phase 2 hardware adapter",
      "",
      "Test connection button: POST /api/v1/settings/payments/test — validates keys against provider sandbox/live.",
      "Webhook URLs shown in UI for merchant to paste into Paystack/Hubtel dashboard:",
      "  https://api.pos.oceancyber.net/api/v1/webhooks/paystack/{tenant_slug}",
      "  https://api.pos.oceancyber.net/api/v1/webhooks/hubtel/{tenant_slug}",
    ],
  },
  {
    title: "E. Offline payment modes",
    body: [
      "Three distinct modes — implement all:",
      "",
      "**E1 — Cash only (default after signup skip)**",
      "No external payment API. Cash tender + change calculation. Full offline sync supported.",
      "",
      "**E2 — Offline MoMo (manual recording)**",
      "Cashier selects MoMo → customer pays on phone outside system → cashier enters:",
      "  • Provider (MTN / Telecel / AT), • Amount, • External reference (optional), • MSISDN last 4 digits",
      "No OTP/API call. Receipt shows 'MoMo — manual confirmation'. Reconciliation report for finance.",
      "Ideal for merchants without Paystack/Hubtel or when network/API down (fallback prompt).",
      "",
      "**E3 — Offline-first POS operations**",
      "Device loses internet: all payment modes above queue locally.",
      "Automated MoMo (API) disabled offline with clear UI message; manual MoMo + cash still work.",
      "On reconnect: sync sales; automated MoMo payments cannot be initiated retroactively for queued sales — cashier completes or converts to manual MoMo.",
      "",
      "Tenant setting: payments.default_mode = cash | manual_momo | automated (requires configured provider).",
    ],
  },
  {
    title: "F. SaaS subscription billing (OceanCyber → merchant)",
    body: [
      "Separate from in-store payments. Billed to tenant owner email via **OceanCyber Paystack account**.",
      "",
      "Plans map to Paystack Plan codes (OceanCyber-owned, not merchant keys):",
      "• pos_starter_monthly — GHS 99",
      "• pos_growth_monthly — GHS 199",
      "• pos_multibranch_monthly — GHS 299",
      "",
      "Trial: 14 days Starter, card/MoMo optional on signup (Paystack subscription with trial_period_days).",
      "Without payment method at trial end: tenant → read_only (view reports, no new sales) until subscribed.",
      "",
      "API:",
      "POST /api/v1/billing/subscribe — { plan_code, payment_method } (Paystack authorization)",
      "GET  /api/v1/billing/status — plan, renews_at, trial_ends_at",
      "POST /api/v1/billing/webhooks/paystack — OceanCyber subscription events (separate webhook secret)",
      "",
      "Enforce plan limits in middleware: branches, users, devices, feature flags.",
    ],
  },
  {
    title: "G. Database — SaaS auth & payment config",
    body: [
      "tenants — add: status (pending|trialing|active|past_due|suspended|cancelled),",
      "  onboarding_step, trial_ends_at, plan_code, paystack_customer_id, paystack_sub_code",
      "",
      "users — add: email_verified_at, verification_token_hash, password_reset_token_hash",
      "",
      "tenant_payment_providers — id, tenant_id, provider (cash|manual_momo|paystack|hubtel),",
      "  is_enabled, config_encrypted (JSON blob), webhook_secret_encrypted,",
      "  last_tested_at, test_status, created_by",
      "",
      "tenant_payment_settings — tenant_id, default_mode, allow_manual_momo_fallback,",
      "  require_supervisor_for_manual_momo_above_ghs (default 500)",
      "",
      "payments — add column: capture_type (automated|manual|cash), external_ref_manual",
      "",
      "audit_payment_config_changes — tenant_id, user_id, provider, action, ip, at",
    ],
  },
  {
    title: "H. REST API — registration & onboarding",
    body: [
      "POST /api/v1/auth/register",
      "  { name, email, phone, password, business_name, accept_terms: true }",
      "  → 201 { user_id, tenant_id, verification_sent: true }",
      "",
      "POST /api/v1/auth/verify-email { token }",
      "POST /api/v1/auth/resend-verification",
      "",
      "GET  /api/v1/onboarding — current step + saved data",
      "PATCH /api/v1/onboarding/step/{1-5} — partial save",
      "POST /api/v1/onboarding/complete — validate all required fields",
      "",
      "POST /api/v1/billing/start-trial — optional at step 3",
      "POST /api/v1/billing/subscribe — Paystack checkout or inline authorization",
    ],
  },
  {
    title: "I. REST API — payment settings",
    body: [
      "GET  /api/v1/settings/payments — enabled providers (secrets masked)",
      "PUT  /api/v1/settings/payments/{provider} — save encrypted credentials",
      "DELETE /api/v1/settings/payments/{provider} — disable provider",
      "POST /api/v1/settings/payments/{provider}/test — sandbox/live ping",
      "PATCH /api/v1/settings/payments/defaults — default_mode, fallback rules",
      "",
      "GET  /api/v1/settings/payments/webhook-urls — copy-paste for merchant dashboards",
      "",
      "At sale time:",
      "GET  /api/v1/checkout/payment-methods — returns enabled methods for branch",
      "POST /api/v1/payments/manual-momo — { sale_id, provider, amount, ref?, msisdn_last4? }",
      "POST /api/v1/payments/momo/initiate — only if paystack/hubtel configured + online",
    ],
  },
  {
    title: "J. Payment UI — POS checkout screen",
    body: [
      "Payment drawer shows only **tenant-enabled** methods:",
      "",
      "[ Cash ]  [ MoMo — Scan/API ]  [ MoMo — Manual ]  [ Card ]",
      "",
      "If no provider configured: show Cash + Manual MoMo only with banner:",
      "'Connect Paystack or Hubtel in Settings to accept automated MoMo.'",
      "",
      "Manual MoMo modal: provider dropdown, amount (pre-filled), reference field, confirm.",
      "Automated MoMo modal: MSISDN input → OTP wait spinner → success/fail.",
      "Offline banner: 'Working offline — automated MoMo unavailable'.",
      "",
      "Admin Settings → Payments: toggle cards per provider, test keys, view webhook URLs.",
    ],
  },
  {
    title: "K. Security — tenant payment secrets",
    body: [
      "Encrypt config_encrypted with ENCRYPTION_MASTER_KEY (32-byte, in KMS/Vault in prod).",
      "Per-tenant derived key: HKDF(master, tenant_id) — limits blast radius.",
      "API never logs decrypted secrets; test endpoint uses in-memory only.",
      "RBAC: only HQ admin role can view/edit payment settings.",
      "Audit every change to tenant_payment_providers.",
      "Webhook routing: tenant_slug in URL path → lookup tenant → verify signature with **that tenant's** webhook secret.",
      "Pen test: cross-tenant payment key access must return 403.",
    ],
  },
  {
    title: "L. Marketing site integration (SaaS)",
    body: [
      "oceancyber.net /products/pos CTAs updated:",
      "• **Start free trial** → https://app.pos.oceancyber.net/signup?utm_source=website",
      "• **Book a demo** → contact form (for Enterprise / assisted onboarding)",
      "",
      "Marketing site does NOT host POS app — only links out to SaaS app domain.",
      "Optional: embed pricing table with deep links per plan ?plan=growth",
      "",
      "Lead tracking: pass utm_* query params through signup → store on tenant for analytics.",
    ],
  },
  {
    title: "M. SaaS sprint backlog additions",
    body: [
      "SAAS-1 Public signup + email verification",
      "SAAS-2 Onboarding wizard UI (5 steps) + API",
      "SAAS-3 OceanCyber Paystack subscription + trial + webhooks",
      "SAAS-4 tenant_payment_providers table + encryption service",
      "SAAS-5 Settings → Payments admin UI (Paystack/Hubtel BYOK)",
      "SAAS-6 Manual MoMo payment flow on POS client",
      "SAAS-7 Payment method gating on checkout (enabled providers only)",
      "SAAS-8 Per-tenant webhook routing + signature verification",
      "SAAS-9 Plan limit enforcement middleware",
      "SAAS-10 past_due / read_only tenant states + billing dunning emails",
      "SAAS-11 Self-serve password reset + invite team emails",
      "SAAS-12 Marketing site CTA links + utm capture",
    ],
  },
  {
    title: "N. Revised sales paths (dual channel)",
    body: [
      "**Self-serve (primary):** Website → Sign up → Trial → Configure payments → Go live.",
      "Target: SMEs, single-branch retail, cash-heavy shops upgrading from notebook.",
      "",
      "**Sales-assisted (secondary):** Website → Book demo → OceanCyber provisions + trains.",
      "Target: Multi-branch, pharmacy, enterprise, merchants needing catalog import help.",
      "",
      "Both paths use same tenant model. Assisted path: OceanCyber admin uses impersonate + pre-fill onboarding.",
      "Enterprise custom pricing: manual plan override on tenant record.",
    ],
  },
  {
    title: "O. Open decisions — updated (v4.0)",
    body: [
      "OD-6 RESOLVED: Self-serve signup YES — core SaaS model.",
      "OD-8 Trial length: 14 vs 30 days — default 14, A/B later.",
      "OD-9 Require Paystack card on trial start vs trial without card — default: trial without card, read_only after.",
      "OD-10 OceanCyber MoMo aggregator tier (optional future): OceanCyber collects on behalf — regulatory review required.",
      "OD-11 POS app domain: app.pos.oceancyber.net vs pos.oceancyber.net — pick one, cookie scope.",
    ],
  },
];
