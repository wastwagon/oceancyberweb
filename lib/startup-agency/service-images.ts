import { siteImagePaths } from "@/lib/seo/site-image-paths";

/** Branded service visuals — shared by homepage hero, services list, and service page heroes. */
export const serviceImages = siteImagePaths.services;
