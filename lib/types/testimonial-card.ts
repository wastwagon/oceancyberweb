/** Serialized testimonial for the home section and loaders. */
export type TestimonialCard = {
  id: string;
  name: string;
  company: string;
  role: string;
  content: string;
  rating: number;
  initials: string;
};
